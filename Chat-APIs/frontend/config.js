// Single place to point the frontend at a different backend
// (e.g. a deployed API URL) without touching script.js.
const DEPLOYED_API_BASE_URL = "https://ai-assignments-yl87.vercel.app";

const APP_CONFIG = {
  API_BASE_URL: DEPLOYED_API_BASE_URL,
};
